@model Authoring.WebRole.Models.ExhibitContentItem

@{
    ViewBag.Title = "Delete";
}

<h2>ExhibitContentItem - Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ExhibitContentItem</legend>

    <div class="display-label">Exhibit</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Exhibit.Title)
    </div>

    <div class="display-label">ContentItem</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ContentItem.Title)
    </div>

    <div class="display-label">Order</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Order)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
