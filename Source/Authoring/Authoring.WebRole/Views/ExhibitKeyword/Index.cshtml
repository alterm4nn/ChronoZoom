@model IEnumerable<Authoring.WebRole.Models.ExhibitKeyword>

@{
    ViewBag.Title = "Index";
}

<h2>ExhibitKeyword - Index</h2>

<p>
    @if (ViewBag.write)
    {
        @Html.ActionLink("Create New", "Create")
    }
</p>
<table>
    <tr>
        <th>
            Exhibit
        </th>
        <th>
            Keyword
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Exhibit.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Keyword.Keyword1)
        </td>
        <td>
        @if(ViewBag.delete) 
        {
            @Html.ActionLink("Edit", "Edit", new { id = item.ExhibitKeywordID })  
            @String.Format("{0}", "|");
            @Html.ActionLink("Details", "Details", new { id = item.ExhibitKeywordID }) 
            @String.Format("{0}", "|");
            @Html.ActionLink("Delete", "Delete", new { id = item.ExhibitKeywordID })
        }
        else if (ViewBag.write)
        {
            @Html.ActionLink("Edit", "Edit", new { id = item.ExhibitKeywordID }) 
            @String.Format("{0}", "|");
            @Html.ActionLink("Details", "Details", new { id = item.ExhibitKeywordID })
        }
        else if (ViewBag.read)
        { 
            @Html.ActionLink("Details", "Details", new { id = item.ExhibitKeywordID })
        }            
        </td>
    </tr>
}

</table>
