namespace Application.Helper.Constants
{
    public class Javascripts
    {
        public const string LastCanvasElement =
            "vc.data('ui-virtualCanvas')._layersContent.children[0].children[vc.data('ui-virtualCanvas')._layersContent.children[0].children.length - 1]";

        public const string Cosmos =
            "vc.data('ui-virtualCanvas')._layersContent.children[0]";

        public const string CosmosChildrenCount =
            "vc.data('ui-virtualCanvas')._layersContent.children[0].children.length";
    }
}